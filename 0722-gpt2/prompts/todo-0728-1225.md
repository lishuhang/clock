# todo-0728-1225.md — PM 通道全流程开发

## 背景
用户要求绕过自动注册（emailPassword 登录 500 的问题），改为**半自动方案**：
1. 新建一个 mail.tm 邮箱管理 Worker，辅助用户快速手动注册 pixmind 账号
2. 适配 pm 通道 Worker，支持导入已注册账号（email+password+uid+token），轮流登录签到

## 整体架构

```
用户浏览器
  ├── mtm.lishuhang.workers.dev (gpt2-mailtm-validator-v1.0.js)
  │   ├── 前端表格：管理 mail.tm 邮箱
  │   ├── 后端：代理 mail.tm API (创建邮箱/读取收件箱)
  │   ├── localStorage: 存储已注册账号列表
  │   └── 导出 JSON: [{email, password, uid, token}]
  │
  └── ai-image.lishuhang.workers.dev (gpt2-worker-pm-v1.1.js)
      ├── 前端：生图界面 (基于 sq-v1.0 改造)
      ├── 设置面板：导入 JSON 账号列表
      ├── 轮流登录签到策略 (基于 v14 的 checkNewDay + selectAccount)
      ├── 余额显示：右上角显示当前账号 credits
      └── 生成代理：pixmind.io API
```

## 阶段一：mail.tm 邮箱管理 Worker (gpt2-mailtm-validator-v1.0.js)

### 1.1 前端表格
- 列：邮箱地址 | 邮箱密码 | 最近邮件标题(点击查看) | 注册状态(按钮→UID)
- 底部按钮：「申请新邮箱」「复制密码」
- 点击邮箱地址 → 一键复制
- 点击最近邮件标题 → 新窗口打开 mail.tm 收件箱（URL 带 auth）
- 点击「已注册」→ 后台用 email+password 登录 pixmind 获取 uid → 显示 uid 并存入 localStorage

### 1.2 后端 API
- `GET /` → 返回 HTML 前端
- `POST /api/mailtm/create` → 代理创建 mail.tm 邮箱（返回 email+password+token）
- `POST /api/mailtm/inbox` → 代理读取收件箱最新邮件（返回标题列表）
- `POST /api/mailtm/messages/:id` → 代理读取单封邮件内容
- `POST /api/pixmind/login` → 用 email+password 登录 pixmind 获取 token+uid
- `POST /api/pixmind/info` → 用 token 获取用户信息（credits 等）

### 1.3 设置页面
- 一键导出已注册账号为 JSON
- JSON 格式：[{email, password, uid, token, registeredAt}]
- 提供复制按钮

## 阶段二：PM 生图 Worker 适配 (gpt2-worker-pm-v1.1.js)

### 2.1 基于 sq-v1.0 改造
- 保留前端 UI 框架（提示词、比例、清晰度、任务列表）
- 修改上游为 pixmind.io
- 修改认证方式为 Bearer token
- 模型改为：gpt-image-2 (默认)

### 2.2 账号管理（核心）
- 设置面板：支持粘贴 JSON 导入账号列表
- 每个账号：{email, password, uid, token, credits, lastCheckinDay, lastUsed, disabled}
- localStorage 存储

### 2.3 轮流签到策略（基于 v14 复原）
- `selectAccount()`: LRU 轮换（最久未用优先），跳过 disabled/credits=0
- `loginAccount(i)`: POST /api/user/login/emailPassword → 获取 token
- `checkinAccount(i)`: POST /api/user/checkin → credits += 330
- `refreshQuota(i)`: GET /api/user/info → 更新 credits
- `checkNewDay()`: 每 60s 检测，新一天自动签到所有账号
- 余额显示：右上角显示当前账号 credits（如 "330 credits / 3 账号"）

### 2.4 生成 API 代理
- `POST /api/generate` → 代理到 pixmind /api/ai/text-to-image
- `GET /api/task-status?taskId=` → 代理到 pixmind /api/ai/image-generate/task-status
- `GET /api/image-proxy?url=` → 代理图片（绕过 CORS）

## 阶段三：部署
- mtm.lishuhang.workers.dev: mailtm validator
- ai-image.lishuhang.workers.dev: pm 生图

## 阶段四：测试
- 用户手动注册流程测试
- JSON 导入 pm worker 测试
- 轮流签到测试
- 生成图片测试

## 注意事项
1. emailPassword 登录 500 问题：在 mailtm validator 中提供登录按钮，如果 500 则显示错误
   但 pm worker 中用 token 工作，不需要频繁登录
2. 事实上用户说"同时需要使用该用户名和密码在后台登录一次 pixmind，获取到 uid"
   如果登录失败，可以在 pm worker 的 checkNewDay 中重试登录
3. 先完成 mailtm validator，让用户能快速注册
4. pm worker 的登录可以在前端或 worker 后端进行
