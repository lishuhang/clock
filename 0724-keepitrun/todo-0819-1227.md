# keepitrun v1.21 增量交接：GLM 内容安全拒答保护

**完成时间：** 2026-08-19 12:27（GMT+8）  
**范围：** 仅修复 `02_combine-gemini.py` 对 GLM HTTP API 内容安全拒答的识别、保底和提示；不改变调度器、RSS 状态文件、`tmp/`、根目录待领取摘要或已发布内容。

## 本轮用户需求（原意保留）

用户反馈 GLM 处理敏感文章时返回：

> API 调用失败: 系统检测到输入或生成内容可能包含不安全或敏感内容，请您避免输入易产生敏感内容的提示语，感谢您的配合。

用户要求检查既有 GLM 回退警告是否涵盖该错误；以一篇指定英文文章进行临时流程测试，确保批量翻译后不会丢失敏感内容的英文标题或摘要；若脚本修改，则热更新并重新打包 v1.21。用户暂时关闭 keepitrun，期望再次启动时保持当前状态。

## 已完成的最小化修改

1. 在 `02_combine-gemini.py` 的 `GLM_REFUSAL_MARKERS` 增加两段新的中文安全拦截特征：`系统检测到输入或生成内容可能包含不安全或敏感内容` 与 `避免输入易产生敏感内容的提示语`。
2. 新增 `GLMContentSafetyRefusal`。GLM HTTP API 返回 HTTP 错误时，脚本读取错误响应体；若匹配上述拒答特征，转为该专用异常，而非将 API 错误文本视作翻译结果。
3. 回退日志针对该异常输出明确提示：GLM 内容安全拒答已识别，正在尝试下一引擎，且不会丢弃英文源标题。
4. 若下一引擎不可用，既有最终保底路径仍返回整个批次的原始英语字符串；因此标题、摘要与链接均不丢失。
5. 更新 `readme.md` 的“翻译与保底策略”“故障排查”和 v1.21 更新日志，明确覆盖 GLM HTTP 安全拦截。

## 隔离测试

指定文章的公开标题与摘要为英文，已仅作为隔离测试夹具使用，未写入生产目录、归档包或日志。测试脚本 `test_glm_sensitive_fallback.py` 覆盖：单条拒答文本、模拟 HTTP 400 安全响应体、仅 GLM 引擎可用时的最终英文保底。`test_glm_sensitive_e2e.py` 覆盖：完整 Markdown 输入到输出流程，在模拟整批 GLM 拒答后，最终摘要保留完整英文标题、摘要和原 URL。

两项测试以及 `python -m py_compile 02_combine-gemini.py` 均通过。测试脚本只在隔离工作副本中存在，发行包必须排除。

## 生产与归档状态

首次尝试连接 Windows 生产机时，远程设备端短暂不可用；用户恢复设备后，已确认生产目录可访问且没有 `AGENTS.md` 约束。生产端通过受校验的脱敏候选包完成热更新：仅替换 `02_combine-gemini.py`、`readme.md` 与本记录；未复制或覆盖 `.env`、`tmp/`、`logs/task_completion.json`、待领取摘要或其他内容目录。部署脚本先建立并验证最小回滚备份 `backups/v1.21-before-glm-hotfix-20260819_1227`（含旧 02 脚本与 README），随后执行生产 `py_compile`。部署时未启动完整调度器。

脱敏 ZIP 已重新构建并通过凭据与受限文件校验，最终 SHA-256 为 `baa297b5608b96b863d58ac594f176b43fd007b735a0b2dcfbc1d02bf870f5fa`。`lishuhang/clock:0724-keepitrun/keepitrun-v1.21.zip` 已更新（提交 `7fca6abc43b2c543912607b40afbb367fbd55bb9`）；本记录已归档至同目录（初始提交 `165de5f0279e434aa3d44a8d2c9cd0568a441848`，随后更新本段写入最终状态）。发行包排除了 `.env`、日志、`tmp/`、测试脚本、`__pycache__`、用户摘要、临时部署脚本与任何凭据。

## 重点注意事项

- GLM 安全拦截可能为整批 HTTP API 错误，而非每一行的自然语言拒答；只在 `preserve_glm_refusals()` 中识别返回内容不足以涵盖此情形。
- 翻译失败时以英文源内容为保底，不得写入 API 错误文本、拒答提示、空标题或丢弃原始 URL。
- 不得在记录、README、代码、测试输出或归档中写入任何实际凭据。
- 本轮不升级版本号：这是 v1.21 的兼容性热修复。 
