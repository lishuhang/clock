# todo-0817-1010.md｜沈腾图表 round1.2 / round2.2 / round3.2 严格重建交接

> 致下一个 agent。时区 GMT+8。本文件为 `todo-0817-0320.md` 的增量交接。
> 凭据安全：不得在仓库、脚本、文档或提交记录中存放 token、API key、密码或个人敏感信息。

---

## 一、用户纠错与本轮目标

用户明确指出此前 `0817-test/round1~3.1` 违反既有规范：原始 skill 的 SVG logo 与阿里巴巴普惠体丢失；加入了未定义的橙色边；私自增加小字与解释性文案；组件没有复用；缺失原文配图中豆瓣影评与小红书评论的 OCR 证据。用户要求严格遵循当前 skill 与 GLM 调整/压缩前版本的原意，制作 `round1.2`、`round2.2`、`round3.2`，并允许只使用隔离浏览器与本地素材。

## 二、已复核的原始规范

已完整核对：

| 规范 | 强制约束 |
| --- | --- |
| `v2.22a-yz-styleguide.html` | round1 白底、大图页脚 horizontal SVG logo、中心 vertical SVG watermark、共享热力/堆叠条/哑铃/文字卡组件。 |
| `v2.22b-yz-styleguide.html` | round2 为 1080×1080；右上只用图形 SVG icon；正文≥40px、辅助≥28px、脚注≥27px、编号≥24px；一图一焦点；超密度拆图而非缩字；复用 `.hm-table`、`.sb-*`、`.db5-*`、`.dumbbell-*`、`.tc-*` 等共享组件。 |
| `v2.22c-yz-styleguide.html` | round3 正式交付为 MP4；16 秒 = 6s 入场 + 4s 静止 + 6s 倒放出场；30fps；四阶为 logo 0s、title 1.5s、图注/脚注 3s、主体 4.5s。 |

本轮实现没有沿用旧轮的 Noto、橙色顶边、渐变背景或自建卡片视觉。`round1.2/assets/yz-brand-sprite.svg` 从原始 v2.22b 的第 396–22109 行精确提取，仅保留 `yz-logo-horizontal`、`yz-logo-vertical`、`yz-logo-icon` 三个真实 symbol。`assets/fonts/` 备有 AliPuHui 400/700/900 本地字体文件。

## 三、数据与 OCR 证据

### 3.1 官方 CSV

新增并采用：

1. `shenteng_works_heatmap.csv`：表1，10 部作品×6 维度，整数百分比。
2. `shenteng_dumbbell_chart.csv`：表3，10 部作品×专业能力/民族国家/社会文化性别，两期比例与源表 `变化Δpp`。
3. 既有 `f1_subdim_chart.csv` 与 `f1_subdim_matrix.csv`：表2，专业能力子维度。

**变化值使用规则**：哑铃图直接显示 `变化Δpp` 原列，不因两期比例的一位小数展示与源表差 0.1pp 而自行覆盖。审计文件为 `0817-test/round1.2-ocr/official_data_audit.json`。

### 3.2 用户提供评论截图 OCR

已按特殊图片规范切片并逐片阅读；切片、manifest、OCR 结果均保存于 `0817-test/round1.2-ocr/`。可用引文：

| 编号 | 平台 | 用于单条图的原文 |
| --- | --- | --- |
| D01 | 豆瓣 | “男性视角的意淫” |
| D02 | 豆瓣 | “好消息是沈腾贡献了最富层次的一次表演，坏消息是成片的质量到底辜负了他。” |
| D03 | 豆瓣 | “各种夸张和尴尬，还用沈腾做幌子。” |
| D04 | 豆瓣 | “看到沈腾的名字立即选座无脑买的。” |
| D05 | 豆瓣 | “作为女性，对于这种中年男人的意淫完全接受不了。” |
| X01 | 小红书 | “腾哥是我们这一辈的星爷😭” |
| X02 | 小红书 | “内娱你欠沈腾一个实至名归的影帝。” |

用户模糊的昵称未被复原。小红书的两条单独评论只被标注为截图评论，不被转写为平台统计结论。

## 四、正式交付物

| 目录 | 数量 | 内容 |
| --- | ---: | --- |
| `0817-test/round1.2/` | 4 HTML + 4 PNG | 表1 六维热力表、表2 专业能力堆叠条、表3 三维度哑铃图、7 条代表评论总图。 |
| `0817-test/round2.2/` | 16 HTML + 16 PNG | 表1 4-3-3 三张、表2 4-3-3 三张、表3 每维度一张、豆瓣 5 条与小红书 2 条单条图。 |
| `0817-test/round3.2/` | 16 HTML + 16 MP4 + 16 JSON | 与 round2.2 一一对应；H.264，1080×1080，30fps，480 帧，16 秒。 |
| `0817-test/round1.2-ocr/` | 审计/切片/记录 | 数据审计、评论 OCR 笔记、切图清单、视频关键帧、重建方案。 |

关键静态及视频检查已通过，见 `round1.2-ocr/final_validation.json`：4/16/16 套文件数量齐全；所有 HTML 使用 AliPuHui、内联真实 symbol、无 Noto、无旧轮 `card::before` 顶边；全部 MP4 为 H.264 1080² 30fps 480 帧 16s。

## 五、可复现流程

```bash
cd /home/ubuntu/clock/0712-yz-styleguide/0817-test
python3 generate_round12_strict.py
./render_round12_png.sh
python3 render_round32_mp4.py
python3 validate_round12.py
```

`render_round32_mp4.py` 使用静态 round2.2 正式容器截帧思路重建 v2.22c 时间线：logo、标题、脚注、主体按 0/1.5/3/4.5 秒分阶出现；静止 4 秒；再倒放出场。该环境无法稳定使用 Chromium CDP，故以原始规范的帧顺序和 ffmpeg 编码独立复现，不依赖用户浏览器或连接器。

## 六、检查与下一步

1. 在推送前再次检查 Git 状态，提交这轮源文件、输出、官方 CSV、字体资产、OCR 取证与本 todo。
2. 当前本轮已经完成。若用户后续只要求某张图调整，必须保持数据源、SVG symbol、AliPuHui 字体、共享组件以及字号下限不变；不得重回此前自建的橙色顶线/私有卡片风格。
3. 如新增文章原文或评论截图，只能追加 OCR 证据和对应的单条图，不替换已有可核验引文。

---

**本 todo 记录时间**：2026-08-17 10:10 GMT+8
