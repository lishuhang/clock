# todo-0812-1240.md — 交接文档（round3.3 修复）

---

## 一、用户原始 prompt（第 11 段 · round3.2 反馈 2 项）

> 工作文件夹 round3.2-bug1.png（repo新上传）：画绿色圈的地方是重叠的地方，感觉你应该适当缩窄线段之间的间距。
> 另一个问题：将动画和静帧时长调整一下：当前淡入1s-静态8s-淡出1s，改为淡入2s-静态4s-淡出2s
> 输出round3.3

### Agent 蒸馏回应

#### 问题 1（img6 标题与标签重叠）：增大 header margin-bottom（已修复）
- **根因**：用户提供的 round3.2-bug1.png 显示 img6 标题"性别批评相关的评论通常打分更低"的"相"字与第一行《战狼2》的 3.40星 标签重叠。
- **修复**：`.chart-header-1x1{ margin-bottom: 48px !important }`（原 24px），让标题区与主体区之间多出 24px 空间。
- 同时缩小 img6 行间距：`.sc6-wrap{ gap: 28px }`（原 36px），`.sc6-row .dumbbell-track{ height: 48px }`（原 52px），确保 5 行仍能放下。
- **验证**：Playwright 检查 title bottom=97, first tag top=227, gap=130px（原 96px）。VLM 确认不再重叠。

#### 问题 2（动画时长 1s-8s-1s → 2s-4s-2s）：调整录屏帧数 + CSS 动画时长（已修复）
- **录屏**：入场 3.5s/105帧 → 2s/60帧；静止 3s → 4s；出场 3.5s → 2s。总时长 10s → 8s。
- **CSS 动画**：所有动画时长压缩以适配 2s 入场窗口：
  - logo: 0.6s → 0.3s
  - title: 0.6s → 0.3s
  - 非图表主体: 0.6s → 0.3s
  - 图表动画: 1.2s → 0.4-0.6s
  - 行 stagger: 0.2s 间隔（原 0.1s）
  - 所有动画在 ~1.8s 内完成
- **验证**：17 个 MP4 全部 8.0s ✓

---

## 二、产物清单

### round3.3（17 个 MP4 + 17 个 HTML，8s 每个）
```
0811-test/round3.3/
├── img{1-6}-part{m}-styled.html + .mp4  # 17 组，8s，无灰边，无重叠
```

### 脚本
```
scripts/
├── build_round3.3_html.py   # HTML 生成器（img6 header margin + 压缩动画CSS）
└── record_round3.3.py       # 录屏脚本（2s入场+4s静止+2s出场）
```

预览 MP4：
- `preview-r33-img6.mp4` — 散点（标题不重叠，8s）
- `preview-r33-img2-pie.mp4` — 饼图（8s）

---

## 三、新增踩坑

### 坑 42：CSS 动画时长需与录屏窗口匹配
- 录屏窗口 2s 内，所有 CSS 动画必须完成
- 如果 CSS 动画总时长 > 录屏窗口，最后一帧不会是完整状态
- **解决**：压缩所有动画 duration 和 delay，确保最后动画在 ~1.8s 完成（留 0.2s buffer）

### 坑 43：header margin-bottom 影响整体布局
- 增大 header margin-bottom 后，chart-body 可用高度减少
- 需同步缩小行间距/轨道高度，否则内容溢出
- **解决**：img6 的 sc6-wrap gap 36→28px，track 52→48px 补偿

---

## 四、未完成事项与下一步

### 等用户做的事
1. **审阅 round3.3** → 用追加 prompt 指出问题，或说"继续"进入步骤 7
2. **中断点 3**：用户审 round3.3 后决定是否进入 v2.21c skill 制作

### 等下一个 agent 做的事（步骤 7）
- **步骤 7**：制作 v2.21c-yz-styleguide.html skill
  - 固化 round3.3 的动画 CSS（2s 入场窗口 + 压缩时长）
  - 固化录屏参数（60帧入场 + 120帧静止 + 60帧出场 = 8s）
  - 固化布局修复（header margin、标签位置、无灰边）

---

**本 todo 完成时间**：2026-08-12 12:40 GMT+8
**下一步**：等用户审 round3.3。用户说"继续"后开始步骤 7（v2.21c skill）。
