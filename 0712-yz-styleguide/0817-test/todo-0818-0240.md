# 沈腾信息图 a.4 / b.4 / c.4：最终交接

> **交接时间**：2026-08-18 02:40（GMT+8）  
> **工作目录**：`0712-yz-styleguide/0817-test`  
> **交接目的**：本轮完成 canonical 静态大图 a.4、1:1 视频插入拆图 b.4 与元素级动画 c.4 的最终重建、复录和阻断式验收。后续任何改动都必须先重跑对应生成器与验证器；不得直接修改 PNG 或 MP4 后跳过验收。

## 1. 本次完成的关键修复

| 成品层级 | 当前状态 | 本轮实际修复 | 验收入口 |
| --- | --- | --- | --- |
| **a.4 / round1.4** | 4 张 canonical HTML + 4 张自适应高度 PNG 已完成 | 从 a canonical 结构生成，修复原有自检对占位符字面量的误判；水印统一为竖版 SVG `#yz-logo-vertical`、420px、opacity 0.10、`z-index:9999`；footer 横版 SVG logo 为 62px；本地 AliPuHui 生效。 | `python3 validate_round14.py` |
| **b.4 / round2.4** | 13 张 1080×1080 HTML + PNG 已重建 | 取消此前不符合下限的 14–30px 可见信息字体。正文统一显式标记为不小于 40px，图例/刻度/元数据不小于 28px，来源脚注 27px，序号 24px；评论保留左引文右正确影片海报；表1严格按 5+5；表2为 4+3+3；表3保持同一 SVG、同一 0–100 比例尺的三维同步哑铃图。 | `python3 validate_round24.py` |
| **c.4 / round3.4** | 13 条 H.264 MP4 已重录 | 取代旧 13.5 秒版本。每条均为 30fps、480帧、16秒（180帧入场 + 120帧静止 + 180帧倒放出场）；截图严格裁切 `.chart-container-1x1`。无中心白色遮罩。 | `python3 validate_round24.py` |

## 2. a.4 成品与验收证据

`round1.4/` 的四张静态大图是：

| HTML / PNG | 内容 | 容器截图尺寸 |
| --- | --- | --- |
| `comments-a-styled` | 7条由 OCR 与原文位置绑定的豆瓣/抖音评论 | 900×1863px |
| `img1-a-heatmap-styled` | 10部作品 × 6维官方热力表 | 900×1934px |
| `img2-a-professional-styled` | 专业能力 5 子维度堆叠条 | 900×2225px |
| `img3-a-dumbbell-styled` | 热映期/近年回看三维哑铃图 | 900×3068px |

`round1.4/canonical_render_validation.json` 记录 Chromium 执行 `yzSelfCheck()` 的结果；四项均为 `pass: true`。`round1.4/validation_report.json` 的 `all_pass` 为 `true`，且按官方 CSV 核验了表1的60个单元格、表2的50个堆叠段、表3的30行两期数据和变化值，并核验七条 OCR 评论的影片、平台、原话和元信息。

## 3. b.4/c.4 成品与验收证据

`round2.4/evidence/final_animation_validation.json` 的 `all_pass` 为 `true`。它阻断式检查 13 张 b.4 HTML/PNG、13 张 c.4 motion HTML、13 条 MP4 以及 `round3.4/recording_manifest.json`。检查项包括：

| 项目 | 强制条件 | 当前结果 |
| --- | --- | --- |
| b.4 自检 | 每张 HTML 均在 Chromium 中执行 `yzSelfCheck1x1()` | 13/13 通过 |
| b.4 画布 | HTML 与预览 PNG 均为 1080×1080 | 13/13 通过 |
| b.4 品牌结构 | 右上 `#yz-logo-icon`、无大型竖版水印、AliPuHui、本地脚注与序号 | 13/13 通过 |
| b.4 内容准确性 | 评论—海报映射；表1官方数值与5+5；表2官方段宽与4+3+3；表3一张同步 SVG | 通过 |
| c.4 动画结构 | 评论海报→打字机、热力单元格 bloom、柱段生长、哑铃线段/端点图元动画，禁止中心遮罩 | 通过 |
| c.4 视频 | H.264、1080×1080、30fps、480帧、16.0秒 | 13/13 通过 |
| c.4 录制 | `capture_selector=.chart-container-1x1`，每个 capture rect=1080×1080 | 13/13 通过 |

抽检报告位于 `round3.4/comment_motion_check.txt`、`round3.4/heatmap_motion_check.txt`、`round3.4/dumbbell_motion_check.txt`。它们确认评论的海报先于评论文字出现、三类图表均无中心白色遮罩或灰边、同步哑铃图按轴/标签→线段→端点的图元顺序出现。

## 4. 可复现命令（必须按顺序）

```bash
cd /home/ubuntu/clock/0712-yz-styleguide/0817-test

# a.4：生成、容器裁切和阻断式验证
python3 generate_round14_canonical.py
python3 validate_round14.py

# b.4：生成严格字号拆图及其元素级动效源
python3 generate_round23.py
python3 generate_round24_motion.py
bash render_round24_preview.sh

# c.4：13条、每条480帧/16秒的容器级录制
python3 record_round34_browser.py

# b.4/c.4 全链路阻断式验证
python3 validate_round24.py
```

## 5. 后续修改的不可违反项

- **不得**把 b.4 的正文字号调低于 40px，辅助文字低于 28px，来源低于 27px，序号低于 24px；发现拥挤必须拆图或重排。
- **不得**把 a.4 的水印改为文字、伪元素、非竖版 SVG，或修改其 420px / 0.10 / 9999 参数；不得把 footer logo 改离 62px。
- **不得**使用白色中心遮罩、`mask`、`clip-path:inset` 或画布裁切模拟 c.4 的出现；必须让真实 DOM/SVG 元素自身动画。
- **不得**再把 c.4 的 16 秒替换为 13.5 秒实验时间轴。任何改动必须保持 180+120+180=480 帧。
- 数据只能来自 `round1.2/data/` 的官方 CSV；评论展示内容只能来自已审计的 OCR 与原文段落映射。禁止写入任何凭据。

## 6. 关键文件索引

| 文件 | 用途 |
| --- | --- |
| `generate_round14_canonical.py` | a.4 canonical 静态 HTML 生成器 |
| `render_round14_canonical.py` | a.4 在 Chromium 自检后裁切 `.chart-container` 的 PNG 渲染器 |
| `validate_round14.py` | a.4 品牌、组件、数据、评论映射与输出阻断式验证 |
| `generate_round23.py` | b.4 严格字号、官方数据、海报/评论结构的源生成器 |
| `generate_round24_motion.py` | 直接在 b.4 页面注入可复用元素动效，输出 c.4 motion HTML |
| `record_round34_browser.py` | c.4 16秒 / 480帧 / 容器裁切录制器 |
| `validate_round24.py` | b.4/c.4 结构、数据、字号、自检、动效、视频与清单阻断式验证 |
| `v1.4_user_requirements_audit.md` | 用户要求、历史偏差、修复策略与视觉抽检审计 |

> 所有本轮中间文件、HTML、PNG、MP4、逐条视频探测 JSON、录制清单、验证报告、视觉抽检和交接文档应一并提交；不要以“体积大”或“已有旧版”为理由遗漏成品。
