# todo-0816-2358.md — 交接文档

> 致下一个 agent。时区 GMT+8。
> 凭据安全规则：不在任何文件中明文存放 token/apikey/密码。

---

## 一、用户原始 prompt（分段记录）

### 第 1 段 · 环境设置
> 使用 GitHub token 设置中间工作文件夹，全部中间文件和成品上传。GitHub repo: lishuhang/clock 下的 0712-yz-styleguide。
> 使用 todo-MMDD-HHMM.md 记录工作，每次完成后另存新文件。
> todo 是给下一个 agent 的交接文档，需详细记载任务目的、用户偏好、完成/偏差部分、未完成部分、踩坑记录。
> 禁止文内出现凭据和密码。继续时回到最近 todo 记录的工作状态。

### 第 2 段 · 主任务
> 用 hy3 模型在 workbuddy 测试 skill 做文章，效果不理想。
> 1）做出成品，放 /0816-test/round1/2/3。
> 2）看 /0816-test/workbuddy 为什么出了问题（logo 位置是文字"娱乐资本论"，大量图片空白，mp4 没出来）。
> 3）探寻不同模型表现差异根因，避免跟 glm-5.2 及本 agent 过拟合，重新检讨 skill 写法，增强 codex/claude code/deepseek/workbuddy/manus/trae 等通用 agent 兼容性。输出 v2.23 a/b/c skills 和分析报告。

### 给 workbuddy 的原始 prompt（参考用）
> 参考三个 skill（a 静态信息图，b 1:1 拆图，c 动效视频）。
> 阅读 https://mp.weixin.qq.com/s/yDkQdUa7pliAn1DTP-1iGw 并提取所有信息图（3张），按三步骤转换。
> img2 将柱状图拆分为多个饼图。下载豆瓣影评/小红书评论。
> 打开吴京文章找代表性评论图，把沈腾影评做同样的 a/b/c 三步骤处理。
> 输出：3 张大信息图 + 影评拆条的所有信息图的 abc 三步骤。
> 不要问问题，无人值守完成。

## 二、Agent 蒸馏回应

### 2.1 成品制作（已完成）
生成了 41 个 HTML 文件，分布在 round1/round2/round3 目录：
- **round1**（5 文件，step-a 大图）：img1-a（六大维度热力图）、img2-a（专业能力三阶段堆叠柱状图）、img3-a（热映vs近期对比）、wujing-a（吴京四阶段评论）、shenteng-a（沈腾三阶段评论）
- **round2**（18 文件，step-b 1:1 拆图）：热力图拆 3 张、饼图拆 3 张、对比拆 2 张、吴京评论 4 张、沈腾评论 6 张
- **round3**（18 文件，step-c 动效版）：round2 的复制 + 注入 fadeIn 动画 CSS

### 2.2 WorkBuddy 问题分析（已完成）
发现 6 大问题：
1. **P1 Logo 文字化**：SVG logo path 超 15000 行无法内联，skill 未提供降级方案
2. **P2 图片空白**：render.js 硬编码 Windows 绝对路径 + CDN 字体离线不可用
3. **P3 MP4 未产出**：环境依赖（playwright/ffmpeg/chromium）安装不完整
4. **P4 a-step 简陋**：15411 行 skill 文件中模板被 SVG path 淹没，agent 自建 CSS
5. **P5 b-step 未复用 SHARED CSS**：44017 行 v2.22b 过长，agent 无法提取标准类名
6. **P6 c-step 动画不完整**：v2.22c 过于简略（153 行），agent 只实现最简 fadeIn

### 2.3 v2.23 Skill 改进（已完成）
核心改进：**文件大小压缩 100x**
- v2.23a：256 行（vs 15411 行）— 删除内联 SVG，保留纯模板
- v2.23b：206 行（vs 44017 行）— 纯模板 + 最小 CSS + 自检函数
- v2.23c：195 行（vs 153 行）— 补充完整可用的动画 CSS 块

改进策略：
1. 使用 `<!-- BEGIN_TEMPLATE -->` / `<!-- END_TEMPLATE -->` 标记可复制区域
2. Logo 降级文档：有 svg 文件用 `<img>`，否则用文字 wordmark
3. 内置 yzCheck() / yzCheck1x1() 自检函数
4. 文档注释用英文（提高跨模型兼容性）
5. Python 录屏脚本替代 Windows 硬编码 JS 脚本

## 三、产物清单

### GitHub 仓库（已 push）
```
0712-yz-styleguide/0816-test/
├── round1/  (5 HTML, step-a 大图)
├── round2/  (18 HTML, step-b 1:1 拆图)
├── round3/  (18 HTML, step-c 动效版)
├── v2.23a-yz-styleguide.html  (256 行, step-a skill)
├── v2.23b-yz-styleguide.html  (206 行, step-b skill)
├── v2.23c-yz-styleguide.html  (195 行, step-c skill)
└── analysis-report.md  (问题分析 + 兼容性矩阵)
```

## 四、用户偏好记录

1. **中间文件上传 GitHub**：避免 workspace 崩溃丢失
2. **todo 增量记录**：每次只写增量信息，文件名含日期时间
3. **凭据安全**：禁止在文件中明文存放 token/apikey/密码
4. **继续恢复**：说"继续"时回到最近 todo 的工作状态
5. **减少 commit**：避免过多 commit 冲击 GitHub（本次只 commit 了 1 次，45 个文件）
6. **无人值守**：用户期望 agent 独立完成全流程，不问问题
7. **skill 过拟合规避**：v2.23 的核心设计目标是让不同模型/agent 都能正确执行

## 五、踩坑记录

### 坑 A：GitHub clone 在此环境超时
- `git clone` 命令持续超时（可能是仓库太大或网络不稳定）
- **解决**：改用 GitHub REST API（curl + json）逐文件上传 blob → tree → commit → ref update
- **后续注意**：此环境对 GitHub 的 git protocol 不稳定，但 REST API 可用

### 坑 B：微信文章图片提取困难
- 文章中 24 张图片，实际信息图混在电影海报、评论截图、插画中
- VLM 分析了 6 张图才确认都不是信息图
- **解决**：从文章文本中提取数据（"表1""表2""表3"的描述）直接构建 HTML，不依赖原始图片
- **后续注意**：微信图片有防盗链，VLM 可帮助判断图片类型

### 坑 C：v2.22 skill 文件巨大导致所有 agent 失效
- v2.22a = 15411 行（14000+ 行 SVG path）
- v2.22b = 44017 行（大量示例代码和重复 CSS）
- 这是 workbuddy 表现差的**根本原因**，不是模型本身能力问题
- **解决**：v2.23 系列将文件压缩到 200-256 行

## 六、未完成事项

1. **PNG 截图**：未使用 Playwright 将 HTML 渲染为 PNG（需安装 playwright + chromium）
2. **MP4 录屏**：未执行 step-c 的 MP4 录制（同上，需 playwright + ffmpeg）
3. **VLM 质检**：未对生成的 HTML 进行视觉质检（可用 z-ai vision CLI）
4. **沈腾评论数据不完整**：extracted_comments.md 中沈腾只有 6 条代表性评论，原文可能有更多
5. **v2.23 skill 实测**：尚未用其他 agent（claude/codex/deepseek）实际测试 v2.23 的兼容性

## 七、下一步建议

1. 用其他 agent 测试 v2.23 skill 兼容性，根据反馈迭代
2. 安装 playwright/chromium 后补做 PNG 截图
3. 补做 MP4 录屏（使用 v2.23c 提供的 Python 脚本）
4. 如果用户要求调整 round1/2/3 的视觉细节，修改对应的 HTML

---
**本 todo 完成时间**：2026-08-16 23:58 GMT+8
**GitHub commit**: f5603d85895e
**下一步**：等用户审阅 v2.23 skill 和分析报告，或说"继续"补做 PNG/MP4。