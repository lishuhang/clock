# todo-0817-0320.md｜round2.1 / round3.1 视频交付交接

> 致下一个 agent。时区 GMT+8。本文为 `todo-0817-0255.md` 的增量记录。
> 凭据安全：不得在仓库文件、提交记录或交接文档中保存 token、API key、密码或个人敏感信息。

---

## 一、用户纠正与最终目标

用户指出：此前完成的 `round3` 只含 HTML 动效与静态预览，但**round3 的交付目标应是将 HTML 动画录成 MP4**，用于插入视频；1:1 拆图必须大字、可在视频中快速阅读。用户允许使用任何可用能力完成，无需手动新增连接器。

已确认此前交接：`0816-test/todo-0816-2358.md` 的未完成项确实包括 MP4 录制；`todo-0812-1807.md` 也明确将“动画 HTML + MP4”作为 round3 产物。此前漏交 MP4 已在本轮修正。

## 二、本轮正式交付

所有新文件位于 `0712-yz-styleguide/0817-test/`：

| 目录 | 数量 | 内容 | 状态 |
| --- | ---: | --- | --- |
| `round2.1/` | 4 张 | 视频插入型 1080×1080 大字静态卡；每屏一个结论，包含 HTML + PNG | 已完成、已检查 |
| `round3.1/` | 4 条 | 对应四张卡的 HTML 动效、PNG 静态预览、**MP4 成片**、每条 MP4 的 probe JSON | 已完成、已检查 |
| `analysis/round31_keyframes/` | 关键帧 | 四条 MP4 在 1.8s / 3.8s 的检查帧 | 已保存、可复核 |

round3.1 的 MP4 文件：

1. `01-comedy-anchor-motion.mp4`：早期三部代表作的喜剧/搞笑比例逐条增长。
2. `02-beyond-comedy-motion.mp4`：`飞驰人生`的喜剧、共情、剧本/节奏/整体三项依次呈现。
3. `03-comedy-not-only-focus-motion.mp4`：`抓娃娃`与`欢迎来龙餐馆`对照，表达喜剧回弹但不是唯一焦点。
4. `04-issue-fade-motion.mp4`：`独行月球` F3 与`超能一家人` F4 从热映期滑向近期回看，呈现议题退潮。

**视频规格**：H.264、1080×1080、24fps、108 帧、4.5 秒、无音频。每条可直接置入剪辑时间线。

## 三、数据和编辑边界

继续只使用已经到位的三份 CSV：`f1_subdim_chart.csv`、`f1_subdim_matrix.csv`、`chart_04_f4_f3.csv`。缺失的 `shenteng_works_heatmap.csv` 与 `shenteng_dumbbell_chart.csv` 仍未补齐，故不可伪造完整表 1 六维热力图或表 3 三维哑铃图。

round2.1/3.1 的数据结论：

1. `夏洛特烦恼`、`羞羞的铁拳`、`西虹市首富`的喜剧/搞笑占比为 78%、76%、76%。
2. `飞驰人生`的喜剧/搞笑、共情/感染力、剧本/节奏/整体为 31%、25%、35%。
3. `抓娃娃`喜剧/搞笑为 73%；`欢迎来龙餐馆`喜剧/搞笑为 4%、演技/声台形表为 25%、剧本/节奏/整体为 57%。
4. `独行月球`的民族国家（F3）为 10.5%→1.2%（−9.3pp）；`超能一家人`的商业资本（F4）为 11.3%→4.8%（−6.5pp）。

## 四、渲染路线与踩坑

初始尝试将 round3.1 HTML 通过浏览器 CDP 逐帧录制。独立 Chromium 调试端口无法稳定启动，已运行浏览器的 WebSocket 连接又受 Origin 限制；这是环境限制，不是交付停止理由。

已改用**独立逐帧渲染器**：`render_round31_mp4.py` 使用本地 Noto CJK 字体和 Pillow 绘制每帧，再用 ffmpeg 编码。该路线不依赖浏览器调试或连接器，既可复现，也避免网络字体、路径或浏览器权限问题。

复现命令：

```bash
cd /home/ubuntu/clock/0712-yz-styleguide/0817-test
python3 generate_video_cards.py
./render_round21_png.sh
python3 render_round31_mp4.py
```

视觉质检发现并修复：

1. 逐帧渲染没有浏览器自然换行，长副标题右侧被裁切；已按 30 字符保守换行。
2. 早期第一张视频中，句末“。”曾独立成行；已合并到上一行，最终中段关键帧无孤立字符。
3. `round3.1/04` 的最终结论帧已验证标题、两行副标题、作品/维度、两期数值、pp 和结论都完整可读。

## 五、下一步

1. 提交并推送本轮 `round2.1`、`round3.1`、渲染器、关键帧、README 更新和本 todo（建议单次 commit）。
2. 如用户补齐热力图与完整哑铃图 CSV，在同一大字/MP4 模板中增加第 5、6 条视频，而不是回退到 0816 的手工数值。
3. 当前没有用户要求的配音或背景音乐；若需要，再单独处理音频，不要在数据视频中自行添加。

---

**本 todo 记录时间**：2026-08-17 03:20 GMT+8
