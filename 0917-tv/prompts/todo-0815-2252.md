# IPTV 清单 0815-2230 增量交接记录

**记录时间：** 2026-08-15 22:52（GMT+8）
**本次目标：** 用户提供 `tv_list-20260815-222710.txt`，要求以此更新此前 list：纳入当前实际可用的 345 源和新增 NHK 官方直连 HLS；将错误归入“大陆”的非线性纯直播源归至“其他”；将 Bloomberg TV+ 移回“国际”；将上传清单中本轮确认不可用的原有 m3u8 输出到独立文件。

## 本次完成内容

| 项目 | 结果 |
|---|---|
| 既有清单复测 | 对上传文件内 133 条 HTTP(S) 地址逐条获取 HLS 清单并验证至少一个媒体资源：75 条传输可用，58 条初测未通过。 |
| 固定动态路由复测 | 对生产 Worker 的 92 条固定 ysp/xuexi/wso 路由复测：13 条传输可用。最终新增到可用清单的非重复路由为 8 条。 |
| 主 345 全目录复测 | 从 v2.10.2 静态 `CATALOG` 枚举 1,692 条固定中转路由，均以 `https://345.lishuhang.com/` 测试 HLS 清单与首个媒体资源；60 条传输可用，其中 41 条不是已有可用条目的重复项，置于独立 `345 中转补充` 分区并保留原始目录标识。 |
| NHK | 用户刚提供的 `https://media-osa.hls.nhkworld.jp/hls/wp/live/master.m3u8` 已验证 `transport_ok`，以 `NHK World Japan` 写入“国际”，无需 345 中转。上传清单中原有 NHK URL 也在本轮传输可用，因此同时保留为另一条线路。 |
| 分类修正 | Bloomberg TV+ 归入“国际”；所有 ABC 与 CGTN feed，以及 NBD AI、Sky News Weather、Wild Earth、熊猫频道等原先落在大陆段的非线性纯直播源归入“其他”。 |
| 不可用输出 | 初测失败的上传原有地址经较晚的路由聚焦复测再去重：若同 URL 在后续复测恢复可用，则仅留在可用文件。最终不可用清单 50 条；可用/不可用两文件 URL 交集为 0。 |
| 格式验证 | 两个输出均以 `#EXTM3U` 开头、文件内 URL 无重复、跨文件 URL 无重复。 |

## 输出文件

| 文件 | 内容 |
|---|---|
| `list_0815-2230.txt` | 最终可用清单，共 125 条：国际 22、香港 10、台湾 10、大陆 29、其他 13、345 中转补充 41。 |
| `list_unavailable_0815-2230.txt` | 上传原有来源中当前未确认可用的 50 条，按相同分类规则整理。 |

两份文件都位于 `/home/ubuntu/clock/0722-tv/`。本机 `0815-iptv` 挂载此前因用户关闭电脑而不可达；如该状态仍未恢复，应以任务附件或远端工作仓库的文件作为交付渠道。

## 可复现审计文件

本次保留的审计输入与输出为：

- `345-current-candidates-0815-2230.txt`、`probe-345-current-0815-2230.json/csv`：固定 ysp/xuexi/wso 路由复测；
- `345-main-catalog-candidates-0815-2230.txt`、`probe-main-345-current-0815-2230.json/csv`：主 345 静态目录 1,692 路由完整复测；
- `nhk-direct-0815-2230.txt`、`probe-nhk-direct-0815-2230.json/csv`：新增 NHK 直连验证；
- `probe-list-0815-2230.json/csv`：用户上传清单的逐项复测。

所有探测均为 HLS 传输层验证：成功表示获得有效清单并读取到至少一个媒体资源；不等于 DRM、加密、地域限制或目标播放器解码体验的最终保证。探测结果未记录或输出动态上游媒体 URL，345 地址保持自定义域名而非 workers.dev。

## 后续注意事项

1. 交付文件中的 `345 中转补充` 使用主目录原始标识（例如 `gtXX`、`fjitvXX`），因为目录没有可靠的面向用户频道名映射；不要自行推断频道名称。
2. 不可用判断存在网络和上游瞬态性；若后续要求更新，应重新运行同样的 HLS 清单及媒体资源探测，而非复用旧状态。
3. 工作树仍存在前序 v2.11 研究阶段的未跟踪文件和一处研究文档修改。提交本轮时只选择列在“可复现审计文件”及两份最终清单和本 todo 的文件，避免意外提交前序候选脚本、动态输入或无关研究材料。
4. 不得在 todo、提交信息、脚本或仓库中写入 token、API key 或动态上游 URL。

---

**本记录仅描述相对于 `todo-0815-2221.md` 的增量。**
